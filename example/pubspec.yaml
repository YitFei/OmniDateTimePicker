name: example
description: An example app to demonstrate the omni_datetime_picker package.
version: 1.0.0+1

environment:
  sdk: ">=3.0.5 <4.0.0"
  #flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  omni_datetime_picker:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
